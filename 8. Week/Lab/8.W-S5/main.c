#include <stdio.h>
/* Q5. Predict the output of following C programs. */

#define max 4
enum sanfoundry {
    a, b = 3, c
};
int main() {
    if ( max != c) {
        printf("Hello");
    } else {
        printf("Welcome");
    }
    return 0;
}
